# Configuration file template
# Modify the values below as deemed nescessary
# See the documentation for more informations
# ------------------------------------------------------------------------------

# Input and Output paths
# ----------------------

# Path to the output directory, will be created if it doesn´t exist
workdir: /path/to/workdir
# Path to the metadata table
metadata: /path/to/metadata
# Path to the directory containing fasta files
fasta_dir: /path/to/dir
# Path to the kraken DB
kraken2_db: /path/to/db

# Max number of threads assigned to single jobs
max_threads_per_job: 1

# Additional parameters
# ---------------------

# Minimum contig length for calculating assembly metrics
min_contig_length: 500
